<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/warm_background"
    tools:context=".SettingActivity">
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <android.support.v7.widget.Toolbar
            android:id="@+id/tb_setting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/button_warm_selector"
            app:navigationIcon="@drawable/ic_action_back"
            app:title="@string/settingsPage"
            app:titleTextColor="@color/warm_text"
            android:layout_marginBottom="16dp"
            android:elevation="4dp"/>

        <Button
            android:id="@+id/btn_locationFre"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/button_warm_selector"
            android:gravity="start|center"
            android:padding="16dp"
            android:text="@string/locationFre"
            android:textColor="@color/warm_text"
            android:textSize="18sp"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"/>

        <Button
            android:id="@+id/btn_report"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/button_warm_selector"
            android:gravity="start|center"
            android:padding="16dp"
            android:text="@string/report"
            android:textColor="@color/warm_text"
            android:textSize="18sp"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"/>

        <Button
            android:id="@+id/btn_db_settings"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/button_warm_selector"
            android:gravity="start|center"
            android:padding="16dp"
            android:text="数据库设置"
            android:textColor="@color/warm_text"
            android:textSize="18sp"
            android:layout_marginBottom="12dp"
            android:elevation="4dp"/>

        <Button
            android:id="@+id/btn_about"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/button_warm_selector"
            android:gravity="start|center"
            android:padding="16dp"
            android:text="@string/about"
            android:textColor="@color/warm_text"
            android:textSize="18sp"
            android:elevation="4dp"/>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>